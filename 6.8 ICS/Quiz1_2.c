#include <stdio.h>
int main() {
    char ch;
    int shift = 4;

    printf("Enter the encoded message (end with ENTER):\n");

    while((ch = getchar()) != '\n'){
        if('A' <= ch <= 'Z'){
            
        }
    }
}